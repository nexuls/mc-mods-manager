# Copy to .env.local (gitignored). Bun loads it automatically when running `bun run dev` in apps/cli.

# Instance directory to manage in development, instead of the current directory.
# MC_MOD_DIR=/path/to/.minecraft
