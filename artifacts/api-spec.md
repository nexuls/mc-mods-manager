## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

X## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

<## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

>## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

z## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

z## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

z## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

Q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

Z## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

1## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

3## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

9## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

5## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

2## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

9## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

2## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

9## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

5## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

2## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

W## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

Q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

9## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

5## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

—## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

1## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

2## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

'## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

J## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

'## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

H## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

\## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

\## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

\## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

9## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

6## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

_## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

B## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

<## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

>## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

<## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

>## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

7## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

7## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

7## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

*## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

R## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

L## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

N## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

4## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

0## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

&## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

=## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

'## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

j## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

.## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

\## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

z## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

"## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

I## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

?## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

[## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

]## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

→## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

{## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

}## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

x## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

m## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

### Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

M## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

D## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

E## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

G## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

,## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

K## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

:## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

b## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

(## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

v## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

)## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

U## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

;## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

A## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

K## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

P## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

O## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

S## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

T## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

n## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

/## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

-## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

f## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

o## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

g## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

`## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

V## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

l## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

d## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

k## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

y## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

w## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

i## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

c## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

h## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

a## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

p## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

C## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

F## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

r## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

q## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

u## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

e## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

s## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

t## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

 ## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

|## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |


## Search & projects

| Method | Path | Description |
|---|---|---|
| GET | `/api/search?provider=modrinth&q=&sort=relevance&page=0` | `{ hits: ProjectHit[], total, page, pageSize, filters }`. Filters to the instance's content kind, loader (plus loaders it can run, OR'ed) and game version (mods only; plugin versions are minimums). Optional `kind`, `category`, `gameVersion`, `loader` overrides; `all=true` ("Show incompatible") drops the loader and version filters. Page size 20. CurseForge gives `PROVIDER_DISABLED` until Phase 6 |
| GET | `/api/projects/:provider/:id` | `Project`: description body (Markdown + HTML, render sanitized), links, gallery, side. `:id` is an id or slug |
| GET | `/api/projects/:provider/:id/versions?all=false` | `{ versions: RankedVersion[] }`, newest first: versions for the instance's loaders and game version (`all=true`: every version), each with `compatible`, `recommended` (architecture §7.3, `services/versions.ts`) and a `note` such as `Fabric build` |

